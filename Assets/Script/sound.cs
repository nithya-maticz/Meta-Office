using UnityEngine;

public class sound : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public sound soundRef;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    private void Awake()
    {
       
    }
}
